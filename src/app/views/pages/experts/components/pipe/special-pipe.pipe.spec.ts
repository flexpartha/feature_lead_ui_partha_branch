import { SpecialPipePipe } from './special-pipe.pipe';

describe('SpecialPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SpecialPipePipe();
    expect(pipe).toBeTruthy();
  });
});
